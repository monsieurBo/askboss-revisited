<%= form_for @personal_message do |f| %>
  <%= hidden_field_tag 'receiver_id', @receiver.id %>

  <div class="field">
  <%= f.label :message %><br/>
  <%= f.text_area :body %>
  </div>

  <%= f.submit "Send Message" %>
<% end %>